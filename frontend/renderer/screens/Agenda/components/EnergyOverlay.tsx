/**
 * EnergyOverlay
 * Camada visual de previsão de energia ao longo do dia.
 * Inicialmente estática, preparada para integração futura.
 */
export function EnergyOverlay() {
  return <div className="energy-overlay" />;
}
